class DataProcessor:
    @staticmethod
    def process_data(data):
        # Process data here (e.g., extract name, position, workstation, age, salary)
        processed_data = ...
        return processed_data
